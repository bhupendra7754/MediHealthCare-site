Step 1: Extract the ZIP file.
  â””â”€ Unzip the project archive to a folder on your machine.

Step 2: Open the folder in VSâ€¯Code.
  â””â”€ In VSâ€¯Code: File â†’ Open Folderâ€¦ â†’ select the unzipped project folder.

Step 3: Install and start the backend.
  3.1 Open a split terminal in VSâ€¯Code.
  3.2 In the first pane, run:
        cd backend
        npm install
        npm start

Step 4: Install and start the frontend and admin.
  4.1 In the second pane, run:
        cd frontend
        npm install
        npm run dev
  4.2 In the third pane, run:
        cd admin
        npm install
        npm run dev

After completing these steps, both the frontend and admin apps plus the backend API will be running on localhost.

â€”

If you have any questions or need help, call us atâ€¯8299431275 or emailâ€¯hexagonsservices@gmail.com.